# 总体架构与代码分析

## 文档维护规则

自 `2026-04-13` 起，本文件与 `开发规范与注释约定.md`、`修改记录.md` 必须同步维护。凡是模块职责、主入口、接口边界、状态机流程发生变化，本文件都要同步更新。

## 当前统一架构

### 顶层执行链

`PRG_MainControl`

1. 先调用 `gvl_fbHMIParaInterface`，处理在线参数与 HMI 编辑缓冲区的同步。
2. 再调用 `gvl_fbMachineController`，统一完成模式判定、自动流程调度、子模块执行和状态汇总。

### 代码树状结构

```text
Code
├─ HMI系统
│  └─ FB_HMIParaInterface.st
├─ 主程序入口
│  ├─ PRG_MainControl.st
│  ├─ PRG_MachineController.st
│  ├─ FB_MachineController.st
│  └─ MachineControlTypes.st
├─ 全局变量
│  └─ GlobalVars.st
├─ 实例管理
│  └─ Instances.st
├─ 模式管理
│  ├─ 手动模式
│  │  ├─ PRG_ManualMode.st
│  │  └─ FB_ManualMode.st
│  ├─ 调模模式
│  │  ├─ PRG_MoldAdjustMode.st
│  │  └─ FB_MoldAdjustMode.st
│  └─ 自动模式
│     ├─ PRG_AutoCycle.st
│     ├─ FB_AutoCycle.st
│     └─ AutoCycleTypes.st
├─ 伺服系统
│  └─ 液压轴
│     ├─ AxisTypes.st
│     └─ FB_HydAxis.st
├─ 合模系统
│  ├─ 锁模
│  │  ├─ ClampTypes.st
│  │  └─ FB_Clamp.st
│  └─ 调模
│     ├─ MoldAdjustTypes.st
│     └─ FB_MoldAdjust.st
├─ 注射系统
│  ├─ 座台
│  │  ├─ NozzleTypes.st
│  │  └─ FB_Nozzle.st
│  ├─ 射胶
│  │  ├─ InjectTypes.st
│  │  └─ FB_Inject.st
│  └─ 储料
│     ├─ MeterTypes.st
│     └─ FB_Meter.st
├─ 顶出系统
│  ├─ 托模
│  │  ├─ EjectTypes.st
│  │  ├─ FB_Eject.st
│  │  └─ FB_EjectMode.st
│  └─ 中子
│     ├─ CoreTypes.st
│     └─ FB_Core.st
├─ 温度系统
│  └─ EK312
│     ├─ EK312Types.st
│     └─ FB_EK312.st
├─ 通用类型
│  └─ CommonTypes.st
└─ 文档
   ├─ 开发规范与注释约定.md
   ├─ 总体架构与代码分析.md
   └─ 修改记录.md
```

### 主控内部分层

`FB_MachineController`

1. 读取 `ST_MachineCommand`、`ST_MachinePara`、`ST_MachineSensor`。
2. 内部调用：
   `FB_ManualMode`、`FB_MoldAdjustMode`、`FB_AutoCycle`、`FB_Clamp`、`FB_MoldAdjust`、
   `FB_Nozzle`、`FB_Inject`、`FB_Meter`、`FB_Eject`、`FB_EjectMode`、`FB_Core`、`FB_EK312`。
3. 对执行模块写入的轴结构，再调用 8 个 `FB_HydAxis` 实例完成轴实例化、限幅和轴层诊断。
4. 汇总形成 `ST_MachineStatus`、各轴命令和温控输出。

### 全局边界

1. `GlobalVars.st` 只保留在线运行必需的共享结构。
2. `Instances.st` 只保留 `gvl_fbHMIParaInterface` 和 `gvl_fbMachineController` 两个顶层实例。
3. 旧版手动/自动中间缓存已经移除，避免旧架构与新架构同时生效。
4. 当前工程中的全局共享对象前缀已统一收口为 `gvl_`。

## 模块职责分析

### HMI 系统

文件：`Code/HMI系统/FB_HMIParaInterface.st`

职责：

1. 管理在线参数与 HMI 编辑缓冲区之间的读回、下发和脏标记。
2. 在机器忙碌时阻止 HMI 直接下发在线工艺参数。
3. 为 HMI 提供统一状态反馈。

### 主程序入口

文件：`Code/主程序入口/*.st`

职责：

1. `PRG_MainControl` 是唯一有效主入口。
2. `MachineControlTypes.st` 定义机器级统一接口。
3. `FB_MachineController.st` 是整机唯一主控编排器。

### 模式管理

文件：`Code/模式管理/*`

职责：

1. `FB_ManualMode`：把机器手动命令路由到各执行模块。
2. `FB_MoldAdjustMode`：把调模模式下允许的动作收口，并强制锁模进入调试方向。
3. `FB_AutoCycle`：只做自动流程步骤调度，不直接驱动轴。
4. `PRG_*` 文件现为兼容占位，不再承担实际执行。

### 伺服系统

文件：`Code/伺服系统/液压轴/*`

职责：

1. `FB_HydAxis`：对模块写入的 `ST_AxisRefHyd` 做最终轴实例化。
2. 统一完成轴号绑定、能力上限回写、命令限幅和轴层报警叠加。
3. 当前已在 `FB_MachineController` 内实例化 8 个轴：
   合模、调模、座台、射胶、储料、托模基础、托模模式、中子。

### 合模系统

文件：`Code/合模系统/*`

职责：

1. `FB_Clamp`：处理开模、合模、低压、高压、调试等动作。
2. `FB_MoldAdjust`：处理模厚调节进退动作。
3. 对应 `*Types.st` 文件维护参数结构与状态枚举。

### 注射系统

文件：`Code/注射系统/*`

职责：

1. `FB_Nozzle`：座台进退控制。
2. `FB_Inject`：射出、保压、射退控制。
3. `FB_Meter`：储料动作控制。
4. 以上 3 个执行块已在 `2026-04-14` 补齐局部变量语义和状态机段落注释，后续修改时应保持同等注释粒度。

### 顶出系统

文件：`Code/顶出系统/*`

职责：

1. `FB_Eject`：基础托模进、保压、退动作。
2. `FB_EjectMode`：定次、震动、停留等组合托模模式。
3. `FB_Core`：中子进退动作。
4. `FB_EjectMode` 通过内部 `FB_Eject` 实例组合更高层托模动作，对外仍透传基础托模块的 Busy/Done/Alarm 与命令输出。
5. 以上 3 个执行块已在 `2026-04-14` 补齐局部变量语义和状态机段落注释，后续修改时应保持同等注释粒度。

### 温度系统

文件：`Code/温度系统/EK312/*`

职责：

1. `FB_EK312`：12 路温控的仿真/控制接口。
2. 输出温控完成、报警和每路状态。

## 本次统一后的关键结论

### 完整性

1. 类型定义、功能块定义和引用关系已做静态核对，当前 `Code` 目录内没有发现缺失的自定义类型或功能块引用。
2. 顶层架构已统一为“单入口 + 单主控”。
3. 新增了独立轴功能块层，执行模块与最终轴实例化已经解耦。
4. 旧版多入口残留逻辑已收敛为兼容占位，避免重复驱动。
5. 全局变量、全局实例命名已统一为 `gvl_` 前缀，顶层边界命名更明确。

### 可执行性

1. 已修复三处明确的 ST 语法问题：`FB_Clamp.st`、`FB_MachineController.st`、`FB_EK312.st` 中的 `END_FOR` 已补齐分号。
2. `PRG_MainControl` 现在已包含 HMI 同步和机器主控调用，单独绑定即可形成完整执行链。
3. `FB_Clamp`、`FB_MoldAdjust`、`FB_Nozzle`、`FB_Inject`、`FB_Meter`、`FB_Eject`、`FB_EjectMode`、`FB_Core` 使用到的轴，已经由 `FB_MachineController` 内部完成实例化。
4. `FB_AutoCycle.st` 已补回本地 `VAR` 状态缓存声明，自动流程块恢复完整的功能块声明结构。
5. `FB_EK312.st` 在停机态和空闲态会主动清零加热输出，温控块的安全收口更明确。
6. 主干 `.st` 文件已统一整理为 `UTF-8 with BOM`，文件头、接口注释和类型注释的显示一致性已收口。
7. 由于当前工作区不包含 PLC 工程任务配置文件，也没有实际 ST 编译器，本项目尚未做编译级验证；任务绑定仍需在工程环境中确认。
8. `FB_Clamp`、`FB_MoldAdjust`、`FB_Nozzle`、`FB_Inject`、`FB_Meter`、`FB_Eject`、`FB_EjectMode`、`FB_Core` 已补齐局部变量定义说明、公共分支意图和状态机分段注释，便于后续按状态排查动作异常。

### 架构风险

1. 如果 PLC 工程里仍把旧版 `PRG_MachineController`、`PRG_ManualMode`、`PRG_MoldAdjustMode`、`PRG_AutoCycle` 绑定到任务，会与新架构说明冲突。
2. 本次已把这些程序改为兼容占位，但工程层仍应检查任务映射，确保只运行 `PRG_MainControl`。

## 后续维护建议

1. 新增模块时，先扩展 `MachineControlTypes.st` 和 `GlobalVars.st` 的统一边界，再扩展 `FB_MachineController`。
2. 修改任何状态机时，必须同步更新本文件中的职责说明和执行链描述。
3. 若后续接入真实 PLC 工程文件，建议补一份“任务配置与下载说明”文档，并在本文件追加编译验证记录。
