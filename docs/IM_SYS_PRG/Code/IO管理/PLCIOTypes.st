(* 
  PLCIOTypes.st
  功能：
  1. 定义现场原始数字量 IO 的统一共享边界 `ST_PLCIO`。
  2. 输入点统一沿用现场 IO 表中的 `X*` 命名；输出点统一沿用现场 IO 表中的 `Y*` 命名。
  3. 当前类型只负责保存“原始点位镜像”，不直接替代 `ST_MachineSensor`、`ST_PanelIO` 或其他工艺层结构。

  维护重点：
  1. 若现场继续扩展数字量点位，必须同时扩展本类型与输入/输出映射块。
  2. 若点位语义变更，必须同步更新 `GlobalVars.st`、`Instances.st`、`PRG_MainControl.st` 与 3 份文档。
*)
TYPE ST_PLCIO :
STRUCT
    (* 原始数字量输入镜像。字段名直接对应现场输入点号，便于电气 IO 表与程序一一对照。 *)
    X0: BOOL;     // 紧急回升原始输入位；由数字量输入映射块从 `IN[0]` 刷新。
    X1: BOOL;     // 低压合模原始输入位；由数字量输入映射块从 `IN[1]` 刷新。
    X2: BOOL;     // 高压合模原始输入位；由数字量输入映射块从 `IN[2]` 刷新。
    X3: BOOL;     // 合模停止原始输入位；由数字量输入映射块从 `IN[3]` 刷新。
    X4: BOOL;     // 电眼输入原始输入位；由数字量输入映射块从 `IN[4]` 刷新。
    X5: BOOL;     // 储料停止原始输入位；由数字量输入映射块从 `IN[5]` 刷新。
    X6: BOOL;     // 拉带停原始输入位；由数字量输入映射块从 `IN[6]` 刷新。
    X7: BOOL;     // 拉带慢速原始输入位；由数字量输入映射块从 `IN[7]` 刷新。
    X10: BOOL;    // 左合模原始输入位；由数字量输入映射块从 `IN[8]` 刷新。
    X11: BOOL;    // 右合模原始输入位；由数字量输入映射块从 `IN[9]` 刷新。
    X12: BOOL;    // 开模停止原始输入位；由数字量输入映射块从 `IN[10]` 刷新。
    X13: BOOL;    // 左托进停原始输入位；由数字量输入映射块从 `IN[11]` 刷新。
    X14: BOOL;    // 左托退停原始输入位；由数字量输入映射块从 `IN[12]` 刷新。
    X15: BOOL;    // 倒带停原始输入位；由数字量输入映射块从 `IN[13]` 刷新。
    X16: BOOL;    // 跳带停原始输入位；由数字量输入映射块从 `IN[14]` 刷新。
    X17: BOOL;    // 二级射出原始输入位；由数字量输入映射块从 `IN[15]` 刷新。
    X20: BOOL;    // 右托进停原始输入位；由数字量输入映射块从 `IN[16]` 刷新。
    X21: BOOL;    // 右托退停原始输入位；由数字量输入映射块从 `IN[17]` 刷新。
    X22: BOOL;    // 座退停止原始输入位；由数字量输入映射块从 `IN[18]` 刷新。
    X23: BOOL;    // 座进停止原始输入位；由数字量输入映射块从 `IN[19]` 刷新。
    X24: BOOL;    // 三级射出原始输入位；由数字量输入映射块从 `IN[20]` 刷新。
    X25: BOOL;    // 中子进停原始输入位；由数字量输入映射块从 `IN[21]` 刷新。
    X26: BOOL;    // 中子退停原始输入位；由数字量输入映射块从 `IN[22]` 刷新。
    X27: BOOL;    // 马达故障原始输入位；由数字量输入映射块从 `IN[23]` 刷新。
    X30: BOOL;    // 安全门原始输入位；由数字量输入映射块从 `IN[24]` 刷新。
    X31: BOOL;    // 模具区域原始输入位；由数字量输入映射块从 `IN[25]` 刷新。
    X32: BOOL;    // 机械手合模原始输入位；由数字量输入映射块从 `IN[26]` 刷新。
    X33: BOOL;    // 机械手托模原始输入位；由数字量输入映射块从 `IN[27]` 刷新。
    X34: BOOL;    // 备用原始输入位；由数字量输入映射块从 `IN[28]` 刷新。
    X35: BOOL;    // 备用原始输入位；由数字量输入映射块从 `IN[29]` 刷新。
    X36: BOOL;    // 射出停止原始输入位；由数字量输入映射块从 `IN[30]` 刷新。
    X37: BOOL;    // 备用原始输入位；由数字量输入映射块从 `IN[31]` 刷新。
    X40: BOOL;    // 备用原始输入位；由数字量输入映射块从 `IN[32]` 刷新。
    X41: BOOL;    // 备用原始输入位；由数字量输入映射块从 `IN[33]` 刷新。
    X42: BOOL;    // 备用原始输入位；由数字量输入映射块从 `IN[34]` 刷新。
    X43: BOOL;    // 备用原始输入位；由数字量输入映射块从 `IN[35]` 刷新。
    X44: BOOL;    // 备用原始输入位；由数字量输入映射块从 `IN[36]` 刷新。
    X45: BOOL;    // 备用原始输入位；由数字量输入映射块从 `IN[37]` 刷新。
    X46: BOOL;    // 备用原始输入位；由数字量输入映射块从 `IN[38]` 刷新。
    X47: BOOL;    // 备用原始输入位；由数字量输入映射块从 `IN[39]` 刷新。
    X50: BOOL;    // 启动命令原始输入位；当前暂定由数字量输入映射块从 `IN[40]` 刷新，并作为 `bStart` 的真实来源。
    X51: BOOL;    // 停止命令原始输入位；当前暂定由数字量输入映射块从 `IN[41]` 刷新，并作为 `bStop` 的真实来源。
    X52: BOOL;    // 复位命令原始输入位；当前暂定由数字量输入映射块从 `IN[42]` 刷新，并作为 `bReset` 的真实来源。
    X53: BOOL;    // 急停命令原始输入位；当前暂定由数字量输入映射块从 `IN[43]` 刷新，并作为 `bEStop` 的真实来源。
    X54: BOOL;    // 备用原始输入位；由数字量输入映射块从 `IN[44]` 刷新。
    X55: BOOL;    // 备用原始输入位；由数字量输入映射块从 `IN[45]` 刷新。
    X56: BOOL;    // 备用原始输入位；由数字量输入映射块从 `IN[46]` 刷新。
    X57: BOOL;    // 备用原始输入位；由数字量输入映射块从 `IN[47]` 刷新。

    (* 原始数字量输出镜像。字段名直接对应现场输出点号，供业务逻辑按现场点号直接写出。 *)
    Y0: BOOL;     // 合模原始输出位；由数字量输出映射块写回 `OUT[0]`。
    Y1: BOOL;     // 座进原始输出位；由数字量输出映射块写回 `OUT[1]`。
    Y2: BOOL;     // 射出原始输出位；由数字量输出映射块写回 `OUT[2]`。
    Y3: BOOL;     // 储料原始输出位；由数字量输出映射块写回 `OUT[3]`。
    Y4: BOOL;     // 射退原始输出位；由数字量输出映射块写回 `OUT[4]`。
    Y5: BOOL;     // 座退原始输出位；由数字量输出映射块写回 `OUT[5]`。
    Y6: BOOL;     // 开模原始输出位；由数字量输出映射块写回 `OUT[6]`。
    Y7: BOOL;     // 左托模进原始输出位；由数字量输出映射块写回 `OUT[7]`。
    Y10: BOOL;    // 左托模退原始输出位；由数字量输出映射块写回 `OUT[8]`。
    Y11: BOOL;    // 引阀原始输出位；由数字量输出映射块写回 `OUT[9]`。
    Y12: BOOL;    // 高压合模原始输出位；由数字量输出映射块写回 `OUT[10]`。
    Y13: BOOL;    // 倒带原始输出位；由数字量输出映射块写回 `OUT[11]`。
    Y14: BOOL;    // 开消阀原始输出位；由数字量输出映射块写回 `OUT[12]`。
    Y15: BOOL;    // 拉带原始输出位；由数字量输出映射块写回 `OUT[13]`。
    Y16: BOOL;    // 中子进原始输出位；由数字量输出映射块写回 `OUT[14]`。
    Y17: BOOL;    // 中子退原始输出位；由数字量输出映射块写回 `OUT[15]`。
    Y20: BOOL;    // 故障报警原始输出位；由数字量输出映射块写回 `OUT[16]`。
    Y21: BOOL;    // 右托模进原始输出位；由数字量输出映射块写回 `OUT[17]`。
    Y22: BOOL;    // 右托模退原始输出位；由数字量输出映射块写回 `OUT[18]`。
    Y23: BOOL;    // 备用原始输出位；由数字量输出映射块写回 `OUT[19]`。
    Y24: BOOL;    // 夹带原始输出位；由数字量输出映射块写回 `OUT[20]`。
    Y25: BOOL;    // 拉带马达原始输出位；由数字量输出映射块写回 `OUT[21]`。
    Y26: BOOL;    // 左模吹气原始输出位；由数字量输出映射块写回 `OUT[22]`。
    Y27: BOOL;    // 右模吹气原始输出位；由数字量输出映射块写回 `OUT[23]`。
    Y30: BOOL;    // 塑机自动原始输出位；由数字量输出映射块写回 `OUT[24]`。
    Y31: BOOL;    // 塑机开模完原始输出位；由数字量输出映射块写回 `OUT[25]`。
    Y32: BOOL;    // 辅助阀原始输出位；由数字量输出映射块写回 `OUT[26]`。
    Y33: BOOL;    // 手动模式原始输出位；由数字量输出映射块写回 `OUT[27]`。
    Y34: BOOL;    // 顶压阀原始输出位；由数字量输出映射块写回 `OUT[28]`。
    Y35: BOOL;    // 电热原始输出位；由数字量输出映射块写回 `OUT[29]`。
    Y36: BOOL;    // 马达原始输出位；由数字量输出映射块写回 `OUT[30]`。
    Y37: BOOL;    // 备用原始输出位；由数字量输出映射块写回 `OUT[31]`。
    Y40: BOOL;    // 备用原始输出位；由数字量输出映射块写回 `OUT[32]`。
    Y41: BOOL;    // 备用原始输出位；由数字量输出映射块写回 `OUT[33]`。
    Y42: BOOL;    // 备用原始输出位；由数字量输出映射块写回 `OUT[34]`。
    Y43: BOOL;    // 备用原始输出位；由数字量输出映射块写回 `OUT[35]`。
    Y44: BOOL;    // 备用原始输出位；由数字量输出映射块写回 `OUT[36]`。
    Y45: BOOL;    // 备用原始输出位；由数字量输出映射块写回 `OUT[37]`。
    Y46: BOOL;    // 备用原始输出位；由数字量输出映射块写回 `OUT[38]`。
    Y47: BOOL;    // 备用原始输出位；由数字量输出映射块写回 `OUT[39]`。
    Y50: BOOL;    // 备用原始输出位；由数字量输出映射块写回 `OUT[40]`。
    Y51: BOOL;    // 备用原始输出位；由数字量输出映射块写回 `OUT[41]`。
    Y52: BOOL;    // 备用原始输出位；由数字量输出映射块写回 `OUT[42]`。
    Y53: BOOL;    // 备用原始输出位；由数字量输出映射块写回 `OUT[43]`。
    Y54: BOOL;    // 备用原始输出位；由数字量输出映射块写回 `OUT[44]`。
    Y55: BOOL;    // 备用原始输出位；由数字量输出映射块写回 `OUT[45]`。
    Y56: BOOL;    // 备用原始输出位；由数字量输出映射块写回 `OUT[46]`。
    Y57: BOOL;    // 备用原始输出位；由数字量输出映射块写回 `OUT[47]`。
END_STRUCT
END_TYPE
